<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gobraille.gobraille">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="sensorLandscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".tentang_kami"
            android:screenOrientation="sensorLandscape"/>
        <activity android:name=".games"
            android:screenOrientation="userLandscape"/>
        <activity android:name=".nilai" />
        <activity android:name=".bantuan"
            android:screenOrientation="sensorLandscape"/>
        <activity
            android:name=".belajar"
            android:label="@string/title_activity_belajar"
            android:screenOrientation="sensorLandscape"
            android:theme="@style/AppTheme" />
        <activity android:name=".keluar"
            android:screenOrientation="sensorLandscape"/>
    </application>

</manifest>